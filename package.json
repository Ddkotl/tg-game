{
  "name": "tg-game",
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "license": "ISC",
  "author": "DKOTL",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "back:dev": "npm run start:dev -w back",
    "front:dev": "npm run dev -w front",
    "dev": "concurrently \"npm run back:dev\" \"npm run front:dev\"",
    "back:build": "npm run build -w back",
    "build": "npm run build --workspaces",
    "docker:start:all": "npm run docker:start --workspaces",
    "docker:stop:all": "npm run docker:stop --workspaces",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
